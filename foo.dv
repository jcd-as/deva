class A
{
	def fcn()
	{
		print( "A.fcn called" );
	}
}

a = new A();
b = new A();

# this does NOT work when there are TWO elements
#m = [a, b];
# but works fine when there is only ONE!!
m = [a];

m[0].fcn();
