
class A
{
	# all classes must have a constructor
	def new()
	{}
	def fcn()
	{
		print( "A.fcn called" );
		local s = ["class A instance"];
		return s;
	}
}

local a = new A();

local m = [a];
m[0].fcn();
