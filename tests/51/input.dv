##################################
# keyword tests
##################################

#l = ['a', 'b', 'c'];

#form( i in l )
#{
#	print( i );
#}

#i = 0;
#whiles( i < l.length() )
#{
#	print( l[i] );
#}

#if( i )
#{
#	print( "aye" );
#}
#elsee
#	print( "nay" );

#importing foo;

# somewhat odd, perhaps, but these produce no error and no code,
# as the compiler just sees them as unused identifiers...
foobar;
breakdance;
continueon;
returned;

formation = 0;
print( formation );

trueness = 1;
print( trueness );

falsehood = 2;
print( falsehood );

whileaway = 3;
print( whileaway );

breakme = 4;
print( breakme );

continue_on = 5;
print( continue_on );

returnofthejedi = 6;
print( returnofthejedi );

null_ness = 7;
print( null_ness );

definition = 8;
print( definition );

iffy = 9;
print( iffy );

else_what = 10;
print( else_what );

into = 11;
print( into );

imported = 12;
print( imported );

classy = 13;
print( classy );

constant = 14;
print( constant );

locally_grown = 15;
print( locally_grown );


