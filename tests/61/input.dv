# test vector appends
local vec = [];
# value types:
local val = null;
vec.append( val );
val = true;
vec.append( val );
val = "foobar";
vec.append( val );
val = 0;
vec.append( val );
# reference types
val = [0,1];
vec.append( val );
val = {0:1};
vec.append( val );
print( vec );
