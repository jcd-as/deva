# const test

#const a = 0xa;
#const a = 0o12;
const a = 0b1010;

m = [];
append( m, a );
# this is okay, m[0] is a new copy of the value '4'
m[0] = 2;

{
	# new scope
	# this would be an error:
#	a = 2;
	const b = 5;
}

b = 1;

# this should generate an error:
#a = 2;

print( str( a ) );
print( str( m[0] ) );
