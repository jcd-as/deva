#import foo/bar;

def foo( s, t )
{
	print( s + t );
}

# test vector built-in method calls
# create a vector
a = [];
# call the built-in append method
a.append( "foo" );
print( a );
#append( a, "foo" );

# test 'method' calls on a map
#a = {};
#a["foo"] = foo;
#a.foo( "this is", " a string" );
#foo( "this is ", "a string" );
